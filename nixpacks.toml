[providers.java]
jdk = 21

[phases.setup]
nixPkgs = ["jdk21", "maven"]

[phases.build]
cmds = ["cd backend && mvn -B -DskipTests clean package"]

[start]
cmd = "cd backend && bash -c 'java -jar target/*.jar'"
